[package]
name = "numu-shared"
version = "0.1.0"
edition = { workspace = true }
license = { workspace = true }
rust-version = { workspace = true }

# Wire DTOs only — the vocabulary the api and data crates share. No logic, no io.
[lib]
name = "shared"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
